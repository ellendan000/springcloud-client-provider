management:
  context-path: /actuator
  security:
    enabled: false
endpoints:
  shutdown:
    enabled: false

security:
  basic:
    enabled: true
    authorize-mode: role
  user:
    name: admin
    password: secret

spring:
  cloud:
    config:
      uri: http://localhost:7001

